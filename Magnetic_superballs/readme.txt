This c++ program any two dipole objet interavtion on magnetic field composed of spheres in 3D with gravity effects takan explicetely into account. In our case we calculate chain behaviour in rotating magnetic filed.
The scripts for movies are also provided
In order to use it user needs to install eigen header library.
